# Billing App

Counter billing UI (cart + checkout).

See the monorepo [README](../README.md) for full setup.

```bash
cp .env.example .env
npm install
npm run dev -- --host 0.0.0.0 --port 5174
```

Log in with a **billing_staff** or **owner** account.
