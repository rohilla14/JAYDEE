# Employee App

Stock check PWA (barcode lookup + stock adjust).

See the monorepo [README](../README.md) for full setup.

```bash
cp .env.example .env
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

Log in with a **stock_staff** or **owner** account.
